server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: deep links and refreshes always serve the app
    location / {
        try_files $uri $uri/ /index.html;
    }
}
